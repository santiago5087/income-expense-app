import { OrderIncomeExpensePipe } from './order-income-expense.pipe';

describe('OrderIncomeExpensePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderIncomeExpensePipe();
    expect(pipe).toBeTruthy();
  });
});
